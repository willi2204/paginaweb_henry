<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Henry | Experiencia Digital</title>
    <style>
        /* Importar fuente premium */
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;700&display=swap');

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Inter', sans-serif;
        }

        body {
            background: #000;
            color: #fff;
            height: 100vh;
            overflow: hidden;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        /* Fondo con esferas de luz animadas */
        .background {
            position: absolute;
            width: 100%;
            height: 100%;
            overflow: hidden;
            z-index: -1;
            background: radial-gradient(circle at 50% 50%, #1a1a2e 0%, #000 100%);
        }

        .sphere {
            position: absolute;
            border-radius: 50%;
            filter: blur(80px);
            opacity: 0.4;
            animation: move 20s infinite alternate;
        }

        .sphere-1 { width: 400px; height: 400px; background: #6c5ce7; top: -10%; left: -10%; }
        .sphere-2 { width: 450px; height: 450px; background: #00d2ff; bottom: -10%; right: -10%; animation-delay: -5s; }

        @keyframes move {
            from { transform: translate(0, 0); }
            to { transform: translate(100px, 100px); }
        }

        /* Contenedor principal (Efecto Cristal) */
        .container {
            position: relative;
            width: 80%;
            max-width: 1000px;
            padding: 60px;
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 40px;
            backdrop-filter: blur(25px);
            text-align: center;
            box-shadow: 0 25px 50px rgba(0, 0, 0, 0.5);
            animation: fadeIn 1.5s ease-out;
        }

        h1 {
            font-size: 5rem;
            font-weight: 700;
            letter-spacing: -3px;
            line-height: 1;
            margin-bottom: 20px;
            background: linear-gradient(to bottom, #fff 30%, #444 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        p {
            font-size: 1.2rem;
            color: #aaa;
            max-width: 600px;
            margin: 0 auto 40px;
            line-height: 1.6;
        }

        /* Botón con brillo */
        .btn-shiny {
            position: relative;
            display: inline-block;
            padding: 18px 45px;
            color: #000;
            background: #fff;
            text-decoration: none;
            font-weight: 700;
            border-radius: 100px;
            font-size: 1.1rem;
            transition: all 0.4s;
            overflow: hidden;
        }

        .btn-shiny:hover {
            transform: scale(1.05);
            box-shadow: 0 0 30px rgba(255, 255, 255, 0.4);
        }

        .btn-shiny::before {
            content: '';
            position: absolute;
            top: 0; left: -100%;
            width: 100%; height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.8), transparent);
            transition: 0.5s;
        }

        .btn-shiny:hover::before {
            left: 100%;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(30px); }
            to { opacity: 1; transform: translateY(0); }
        }

        /* Ajuste para móviles */
        @media (max-width: 768px) {
            h1 { font-size: 3rem; }
            .container { width: 95%; padding: 40px 20px; }
        }
    </style>
</head>
<body>

    <div class="background">
        <div class="sphere sphere-1"></div>
        <div class="sphere sphere-2"></div>
    </div>

    <div class="container">
        <h1> HENRY </h1>
        <p>Arquitecto de experiencias digitales. Elevando el desarrollo web a través de un diseño minimalista y código de alto rendimiento.</p>
        <a href="#" class="btn-shiny" onclick="mostrarSorpresa()">Entrar al Universo</a>
    </div>

    <script>
        function mostrarSorpresa() {
            alert("✨ Estás navegando en una web de alto nivel. ¡Bienvenido al futuro, Henry!");
        }
    </script>
</body>
</html>
